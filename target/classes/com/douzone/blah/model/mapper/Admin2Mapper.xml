<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.douzone.blah.dao.Admin2DAO">

	<!-- 로그인 -->
	<select id="submit" parameterType="hashmap">
		select admin_id,admin_password,enabled,authority from admin2 where
		admin_id=#{admin2_id}
	</select>
</mapper>